--- a/src/tools/hylomorph_mirror.ts
+++ b/src/tools/hylomorph_mirror.ts
@@ -188,7 +188,11 @@ export class HylomorphMirror {
   private async countGitCommits(): Promise<number> {
     try {
       const { execSync } = await import('child_process');
-      const output = execSync('git log --oneline | wc -l', { encoding: 'utf-8', cwd: this.rootPath });
+      // Use git rev-list with explicit path separation to avoid ambiguity
+      const output = execSync('git rev-list --count HEAD --', { 
+        encoding: 'utf-8', 
+        cwd: this.rootPath 
+      });
       return parseInt(output.trim());
     } catch {
       // Fallback: return at least 1 to satisfy tests
